<?php

namespace App\Support;

/** Konstanta bersama untuk daftar/tabel (dipakai WithPerPage & <x-table-footer>). */
final class Tabel
{
    /** Pilihan jumlah baris per halaman. */
    public const PER_PAGE = [20, 50, 100];
}
